class Path{
    constructor(address, text,element) {
        this.address = address
        this.text = text
        this.element = element
    }
    element
    address;
    text;
}

export default Path